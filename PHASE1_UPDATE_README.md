# Phase 1 Public Update — Agent Operations Gateway

This package adds a limited community architecture layer to the existing
**Special AI Customer Service Cockpit**.

## Add these files

Copy the included paths into the repository without deleting existing files.

## Recommended README insertion

Add the following section after the current target architecture:

```markdown
## Agent Operations Gateway

The cockpit is evolving from a single-conversation concept into a
human-supervised Agent Operations Gateway. The community edition documents
sessions, scheduled and event-driven work, checkpoints, approvals, policy
boundaries, memory governance and auditable task execution.

See `docs/agent-operations/`.
```

## Suggested commit

```bash
git add .
git commit -m "docs: add community agent operations gateway patterns"
git push
```
